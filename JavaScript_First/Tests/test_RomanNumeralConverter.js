import('../RomanNumeralConverter.js');
// I used this file to test parts of the project during development to keep my main file clean.

for (let i = 1; i < 3; i++) {
    console.log(`For value: ${i} oneToThree function returns: ${oneToThree(i)}`); 
}